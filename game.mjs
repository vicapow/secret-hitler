// @flow

// This is the game state machine. The game state can only be changed through `event` messages.

/* :: import type { Game, Message } from './types'; */

export default function reducer(state /* : Game */, message /* : Message */) /* : Game */ {
  return state;
}
